---
const { name, src } = Astro.props
---
<div class="flex flex-col">
    <h2 class="text-3xl bg-fuchsia-700/40 text-slate-50 text-center p-3">
        {name}
    </h2>
    <picture>
        <img src={src} class="min-w-full h-75 object-cover" alt={name} />
    </picture>
</div>
